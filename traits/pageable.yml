queryParameters:
  pagesize:
    description: Number of elements per page
    type: integer
    required: false
    example: 20
    default: 0
  limit:
    description: Limit the number of elements on the response
    type: integer
    required: false
    example: 80
    default: 10